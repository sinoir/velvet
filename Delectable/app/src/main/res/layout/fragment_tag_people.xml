<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/with_container"
        android:layout_alignParentTop="true"
        android:choiceMode="multipleChoice"
        android:divider="@null" />

    <RelativeLayout
        android:id="@+id/with_container"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tag_row_height"
        android:layout_alignParentBottom="true"
        android:background="@color/d_chestnut"
        android:visibility="gone">

        <com.delectable.mobile.ui.common.widget.FontTextView
            android:id="@+id/with_text"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:layout_marginLeft="@dimen/activity_horizontal_margin"
            android:gravity="center_vertical"
            android:textColor="@color/d_white"
            android:textSize="@dimen/tag_with_people_text_size"
            app:fontName="whitney_book" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:layout_marginRight="@dimen/activity_horizontal_margin"
            android:src="@drawable/ic_next" />
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:visibility="visible">

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/general_row_height"
            android:layout_marginTop="@dimen/spacing_16" />

        <com.delectable.mobile.ui.common.widget.FontTextView
            android:id="@+id/empty_text_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:padding="@dimen/spacing_32"
            android:text="@string/tag_friends_empty_state_text"
            android:textColor="@color/d_medium_gray"
            android:textSize="@dimen/text_size_medium"
            android:visibility="gone"
            app:fontName="whitney_book" />


    </RelativeLayout>
</RelativeLayout>
